# Wallet that deploys the contract. Get testnet MON from https://testnet.monad.xyz
DEPLOYER_PRIVATE_KEY=

# Wallet the backend uses to call checkIn(). Can be the same as deployer for
# the hackathon, but keep it a throwaway key either way — never your main wallet.
VERIFIER_ADDRESS=
VERIFIER_PRIVATE_KEY=
