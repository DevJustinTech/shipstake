MONAD_RPC_URL=https://testnet-rpc.monad.xyz
CONTRACT_ADDRESS=
VERIFIER_PRIVATE_KEY=
GITHUB_TOKEN=
DB_PATH=shipstake.db

# GitHub OAuth app (for private-repo verification) — create one at
# https://github.com/settings/developers with callback URL
# <backend url>/auth/github/callback (e.g. http://localhost:8000/auth/github/callback)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# Where the OAuth callback should send the user back to
FRONTEND_URL=http://localhost:3000
